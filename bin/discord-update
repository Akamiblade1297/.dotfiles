#!/bin/bash
wget "https://discord.com/api/download?platform=linux&format=tar.gz" -O /tmp/discord.tar.gz &&
rm -rf $HOME/Desktop/Discord &&
tar -xzvf /tmp/discord.tar.gz && 
rm -rf /tmp/discord.tar.gz &&
mv Discord $HOME/Desktop &&
discord
