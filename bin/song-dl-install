#!/bin/bash
for [[ $(ls $HOME/Music | wc -l) -ne $(cat $HOME/.config/mpd/song-dl.list | wc -l) ]]; do
	cat $HOME/.config/mpd/song-dl.list | while read -r line; do
	    IFS=" " read -r link name <<< $line
	    if ! [[ -f "$HOME/Music/$name.mp3" ]]; then
		song-dl $link "$name" 0
	    fi
	done
done
